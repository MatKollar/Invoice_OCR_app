import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  card: {
    width: "30%",
    margin: "0 auto",
    marginTop: 50,
    minWidth: 250,
  },
  textFields: {
    margin: "0 auto",
    width: "60%",
  },
});
