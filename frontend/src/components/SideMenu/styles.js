import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    rootContainer: {
        color: "white",
        width: "15%",
        backgroundColor: "#222222",
        textAlign: "center"
    },
    linkContainer: {
        marginTop: 40,
    },
    link: {
        color: "white",
    },
});