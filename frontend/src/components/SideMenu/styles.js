import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    rootContainer: {
        color: "white",
        width: "15%",
        height: "100vh",
        backgroundColor: "#222222",
        textAlign: "center"
    },
});