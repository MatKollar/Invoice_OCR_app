import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  rootContainer: {
    width: '50%',
    margin: "0 auto",
    marginTop: 30,
  },
  textField: {
    backgroundColor: "white",
    borderRadius: 10,
  },
});
