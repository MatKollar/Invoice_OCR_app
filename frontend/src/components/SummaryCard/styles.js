import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  rootContainer: {
    margin: "0 auto",
    marginTop: 30,
  },
  textField: {
    backgroundColor: "white",
    borderRadius: 10,
    width: "50%",
  },
});
