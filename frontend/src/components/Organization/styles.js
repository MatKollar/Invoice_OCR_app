import { makeStyles } from "@mui/styles";
import { height } from "@mui/system";

export const useStyles = makeStyles({
  rootContainer: {
    textAlign: "center",
    color: "black",
  },
  table: {
    margin: "auto",
    width: "80%",
    height: "100%",
  },
});
