import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  rootContainer: {
    textAlign: "center",
    color: "black",
  },
  table: {
    margin: "auto",
    width: "80%",
    height: "100%",
  },
  activeButton: {
    marginTop: "20px",
    textAlign: "center",
  },
});
