import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    pageWrapper: {
        display: "flex",
    },
    contentWrapper: {
        width: "100%",
        backgroundColor: "#B2B2B2",
    },
    content: {
        textAlign: "center",
    }
});